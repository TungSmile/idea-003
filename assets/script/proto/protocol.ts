import { _decorator, Component, Node, Socket ,sys} from 'cc';
const { ccclass, property } = _decorator;

@ccclass('protocol')
export class protocol extends Component {
    start() {

    }

    update(deltaTime: number) {

    }
}

